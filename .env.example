# Optional. If set, the app uses this Google OAuth client ID and skips the
# "paste your client ID" step in Settings. Copy this file to .env.local.
# Never commit .env.local if the repo is someone else's to deploy.
VITE_GOOGLE_CLIENT_ID=
